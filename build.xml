<project name="ParquetPlugin" default="dist" basedir=".">
    <description>
        Build the Parquet Output plugin for Pentaho Data Integration.
    </description>
  <!-- set global properties for this build -->
  <property file="build.properties" />
  <property file="build.number" />
  <property name="version" value="TRUNK-SNAPSHOT/>

  <target name="init">
    <!-- Create the time stamp -->
    <tstamp/>
    <!-- Create the build directory structure used by compile -->
    <mkdir dir="${build}"/>
  </target>

  <!-- software revision number -->

  <target name="increment_the_build_number" depends="init">
	<propertyfile file="build.number"
				  comment="Build Number for ANT. Edit not!">
	<entry key="buildnumber" type="int" 
		 operation="+" 
		 default="1" />
	</propertyfile>
  </target>

  <target name="buildinfo" depends="increment_the_build_number">
    <tstamp>
        <format property="builtat" pattern="MM/dd/yyyy hh:mm aa" timezone="America/New_York"/>
    </tstamp>        
    
    <exec executable="whoami" outputproperty="whoami"/>

    <propertyfile file="${build}/buildinfo.properties"
        comment="This file is automatically generated - DO NOT EDIT">        
        <entry key="buildtime" value="${builtat}"/>
        <entry key="build" value="${buildnumber}"/>
        <entry key="builder" value="${whoami}"/>
        <entry key="version" value="${version}"/>
    </propertyfile>
  </target>
  
  <target name="compile" depends="buildinfo"
        description="compile the source " >
	
    <!-- Compile the java code from ${src} into ${build} -->
    <javac debug="on" includeantruntime="false" target="1.6" source="1.6" srcdir="${src}" destdir="${build}">
		<classpath>
			<fileset dir="${pentahoclasspath}/" >
				<include name="**/*.jar"/>
			</fileset>
			<fileset dir="${pentahoswtclasspath}/" >
				<include name="**/*.jar"/>
			</fileset> 
			<fileset dir="${pentahobigdataclasspath}/" >
				<include name="**/*.jar"/>
			</fileset>
      <fileset dir="${pentahobigdataclasspath}/../" >
        <include name="**/*.jar"/>
      </fileset>
      <fileset dir="." >
        <include name="*.jar" />
      </fileset>
		</classpath>
	</javac>
	<copy todir="${build}">
          <fileset dir="${src}" includes="**/*.png **/*.xml **/*.xul **/*txt **/*properties"/>
    </copy>
  </target>
  
  <target name="dist" depends="compile"
        description="generate the distribution" >
    <!-- Put everything in ${build} into the MyProject-${DSTAMP}.jar file -->
	<mkdir dir="${dist}"/>
	<mkdir dir="${dist}/ParquetPlugin"/>
  <mkdir dir="${dist}/ParquetPlugin/lib"/>
    <jar jarfile="${dist}/ParquetPlugin/ParquetPlugin.jar" basedir="${build}"/>
<!--	<copy file="plugin.xml" todir="${dist}/ParquetOutputPlugin"/>-->

	<copy file="LICENSE" todir="${dist}/ParquetPlugin"/>
	<copy file="README.md" todir="${dist}/ParquetPlugin"/>
  <copy file="avro-1.6.2.jar" todir="${dist}/ParquetPlugin/lib"/>
  <copy file="parquet-hadoop-bundle-1.5.0.jar" todir="${dist}/ParquetPlugin/lib"/>
  <copy file="parquet-avro-1.5.0.jar" todir="${dist}/ParquetPlugin/lib"/>
	<echoxml file="${dist}/ParquetPlugin/version.xml">
		<version branch="${branch}" buildId="${buildnumber}">${version}</version>
	</echoxml>
  </target>

  <target name="clean"
        description="clean up" >
    <!-- Delete the ${build} and ${dist} directory trees -->
    <delete dir="${build}"/>
    <delete dir="${dist}"/>
  </target>
</project>